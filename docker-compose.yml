version: '3.8'

services:
  wildfire-app:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - API_URL=http://localhost:8000
